name: security-scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly Monday 06:00 UTC — catches new CVEs in pinned deps
    - cron: "0 6 * * 1"

jobs:
  python:
    name: Python (bandit + pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install scanners
        run: |
          python -m pip install --upgrade pip
          pip install bandit[toml] pip-audit
      - name: bandit — static security analysis
        run: bandit -r backend --severity-level medium --confidence-level medium --exclude backend/services/code_runner_child.py,backend/services/sandbox.py
      - name: pip-audit — dependency CVE check
        run: pip-audit --requirement backend/requirements.txt --strict

  semgrep:
    name: Semgrep (OWASP Top 10)
    runs-on: ubuntu-latest
    container:
      image: returntocorp/semgrep:latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: semgrep scan
        run: |
          semgrep scan \
            --config p/python \
            --config p/typescript \
            --config p/owasp-top-ten \
            --exclude 'frontend/dist/**' \
            --exclude 'frontend/node_modules/**' \
            --error
        env:
          # SEMGREP_APP_TOKEN is optional; without it `semgrep ci` falls back
          # to public rulesets only — that is fine for OSS projects.
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

  frontend:
    name: Frontend (npm audit)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install deps
        run: npm ci --legacy-peer-deps
      - name: npm audit — high+ severity gate
        run: npm audit --audit-level=high

  secrets-scan:
    name: Secret scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - name: gitleaks
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Pin the scanner. Left to itself the action asks the old
          # zricethezav/gitleaks repo for the latest release, which has stopped
          # moving at 8.24.3 — so "latest" silently means a binary from early
          # 2025. Pinning states which binary the exemptions in .gitleaks.toml
          # were verified against, and stops the version drifting under us.
          GITLEAKS_VERSION: 8.30.1

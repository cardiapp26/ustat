# Development / testing dependencies.
# Install with:  pip install -r requirements-dev.txt
# (pulls in the runtime deps via the -r include below, then adds test tooling)
-r requirements.txt

hypothesis==6.155.1          # Property-based testing for simulation studies
pytest==9.0.2
pytest-cov==7.1.0
httpx
pytest-httpx>=0.30.0

